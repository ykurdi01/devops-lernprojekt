# Damit man die App auch ohne Kubernetes schnell lokal testen kann.
services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "5000:5000"
    environment:
      APP_VERSION: "0.1.0"
