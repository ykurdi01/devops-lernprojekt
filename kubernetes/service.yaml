apiVersion: v1
kind: Service
metadata:
  name: lernprojekt-service
  namespace: lernprojekt
spec:
  type: NodePort
  selector:
    app: lernprojekt-app
  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080
